document.addEventListener('DOMContentLoaded', () => {
const gameBoard = document.querySelector('.gameBoard');
let rows = Array.from(document.querySelectorAll('.gameBoard div'));
note = [0, 1, 2]

function createNote(){
  console.log(note.isArray)
}

createNote()
})
